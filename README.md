# Venturenix Next-Gen AI Development Class Cohort #5

## 作業 1：AI 新聞摘要系統 (初學者友善版本)

這個 Jupyter Notebook (homework01.ipynb) 是一個專為初學者設計的 AI 新聞摘要系統。它展示了如何使用 Python 程式碼來：

- **安全地載入環境變數**：學習如何使用 `.env` 檔案管理 API 金鑰。
- **整合外部 API**：從 Serper API (Google Search API) 獲取即時新聞資料。
- **使用大型語言模型 (LLM)**：透過 pydantic-ai 框架與 Google 的 Gemini 模型互動，自動生成專業的新聞摘要。
- **逐步理解程式碼**：每個步驟都附有詳細的說明、程式碼註解和 Markdown 解釋，讓初學者能夠輕鬆理解整個流程。

### 運行步驟：

1. **環境設定**：確保您的 `.env` 檔案中包含 `SERPER_API_KEY` 和 `OPENROUTER_API_KEY`。
2. **執行筆記本**：按順序運行 `homework01.ipynb` 中的每個單元格。

這個項目旨在提供一個實用的範例，幫助您掌握 AI 應用開發的基本概念和流程。

***
*此 README.md 由 AI 生成。*
