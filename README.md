# Venturenix Next-Gen AI Development Class Cohort #5

## lesson02.ipynb - Python 物件導向程式設計入門 (Object-Oriented Programming for Python Beginners)

這個 Jupyter Notebook (`lesson02.ipynb`) 旨在向 Python 初學者介紹物件導向程式設計 (OOP) 的核心概念。透過實作一個「月餅購物車」的範例，您將會學習到以下重要的 OOP 概念：

- **Class (類別)**：如何定義 `BaseMoonCake`、`VegMoonCake`、`LotusSeedMoonCake` 和 `ShoppingCart` 等 Class 來建立藍圖。
- **Object (物件)**：如何從 Class 建立實際的 `MoonCake` 和 `ShoppingCart` Object。
- **Attribute (屬性)**：Object 如何儲存資料，例如 `name`, `price`, `discount`, `is_vegetarian` 和 `expiry_date`。
- **Method (方法)**：Object 如何執行動作，例如 `add()`, `remove()` 和 `get_total_price()`。
- **Inheritance (繼承)**：`VegMoonCake` 和 `LotusSeedMoonCake` 如何從 `BaseMoonCake` 繼承屬性 (attribute) 和方法 (method)，並加入自己獨特的特性。
- **Polymorphism (多型)**：不同的 `MoonCake` Object 如何對相同的操作 (例如 `print(cake)`) 產生不同的行為。
- **Encapsulation (封裝)**：如何將資料 (attributes) 和操作資料的程式碼 (methods) 綁定在一起。

這個 Notebook 也包含了月餅樣本的生成，並模擬了購物車的活動，讓您能實際觀察這些 OOP 概念的運作。透過這個範例，您可以更好地理解如何在 Python 中使用 OOP 來建構更具結構化和可維護性的程式碼。

---
*本說明文件由 AI 生成。 (This documentation is AI-generated.)*
