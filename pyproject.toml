[project]
name = "cohort5"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "nest-asyncio>=1.6.0",
    "pydantic-ai-slim[openai]>=1.0.10",
    "python-dotenv>=1.1.1",
]
